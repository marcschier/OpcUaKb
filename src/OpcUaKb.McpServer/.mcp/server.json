{
  "name": "opcua-kb-mcp",
  "version": "4.5.0",
  "description": "OPC UA Knowledge Base MCP Server — 17 tools: structured NodeSet search, RAG Q&A, validate/compliance/compare NodeSets, modelling guidance, the OPC UA Profile graph + conformance tools, and asynchronous live-server → companion-spec projection, across all OPC UA specifications and companion specs.",
  "publisher": "marcschier",
  "repository": "https://github.com/marcschier/OpcUaKb",
  "tools": [
    {
      "name": "search_nodes",
      "description": "Search OPC UA NodeSet nodes with structured filters (node class, spec, parent type, modelling rule). Version-aware."
    },
    {
      "name": "get_type_hierarchy",
      "description": "Get ObjectType inheritance chain with declared/inherited member counts."
    },
    {
      "name": "get_spec_summary",
      "description": "Get pre-computed NodeSet statistics for a companion spec or all specs."
    },
    {
      "name": "search_docs",
      "description": "Full-text search across OPC UA specification HTML pages and tables. Version-aware."
    },
    {
      "name": "search_docs_rag",
      "description": "RAG Q&A grounded by the OPC UA spec corpus (KB retrieve + GPT-4o)."
    },
    {
      "name": "count_nodes",
      "description": "Faceted aggregation by node_class, spec_part, modelling_rule, or data_type."
    },
    {
      "name": "list_specs",
      "description": "Ranked catalog of all indexed companion specs and CloudLib NodeSets."
    },
    {
      "name": "validate_nodeset",
      "description": "Validate NodeSet XML (inline ≤30KB, blob: ref, or https URL) against OPC UA + OPC 11030 best practices."
    },
    {
      "name": "compare_versions",
      "description": "Compare two spec versions, classify changes as backward-compatible or breaking."
    },
    {
      "name": "check_compliance",
      "description": "Check a NodeSet implementation (inline, blob: ref, or https URL) against a companion spec."
    },
    {
      "name": "suggest_model",
      "description": "Suggest OPC UA information model design based on a domain description."
    },
    {
      "name": "list_profile_groups",
      "description": "List OPC UA Profile groups (spec + version, e.g. UACore 1.05) with profile/conformance-unit counts and release-status mix."
    },
    {
      "name": "get_profile",
      "description": "Get a profile's graph neighborhood: status, required/optional conformance units, included profiles, and reverse 'included by' edges."
    },
    {
      "name": "query_profiles",
      "description": "Filter profiles by name fragment, group, and release status, then expand includes / included_by relationships."
    },
    {
      "name": "check_profile_conformance",
      "description": "Traverse the conformance graph in expand / satisfy / diff modes over transitive required/optional conformance units."
    },
    {
      "name": "create_companion_projection",
      "description": "Submit a live-server NodeSet2 (inline / blob: ref / allow-listed URL) to a durable async job that projects it onto official companion specs; returns a job ID."
    },
    {
      "name": "get_companion_projection",
      "description": "Poll a companion projection job and retrieve the generated NodeSet2, node-to-node mapping JSON/CSV, reports, and ZIP bundle."
    }
  ],
  "transport": {
    "stdio": {
      "command": "opcua-kb-mcp",
      "args": ["--stdio"],
      "env": {
        "SEARCH_ENDPOINT": { "description": "Azure AI Search endpoint URL", "required": true },
        "SEARCH_API_KEY": { "description": "Azure AI Search API key", "required": true }
      }
    },
    "http": {
      "url": "https://opcua-kb-mcp-server.lemoncliff-5369bab2.westus3.azurecontainerapps.io/",
      "headers": {
        "api-key": { "description": "Application api-key. Optional for tool calls when the server allows anonymous access (MCP_REQUIRE_AUTH=false); required for POST /upload-nodeset and GET /mapping-artifacts.", "required": false }
      }
    }
  }
}
